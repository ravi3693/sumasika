    <%= render :partial => 'cras/home' %>

      <!-- HEADLINE

      ================================= -->

      <%= render :partial => 'cras/headline' %>

      <!-- DESCRIPTION
      ================================= -->
      
     <%#= render :partial => 'cras/pricing' %>

      <!-- BENEFITS
      ================================= -->
      <%= render :partial => 'cras/benfites' %>

      <!-- HOW IT WORKS
      ================================= -->
      <%= render :partial => 'cras/howitworks' %>

      <!-- NUMBERS
      ================================= -->
      
      <% unless @expert.empty? %>

      <%= render :partial => 'cras/numbers' %>

      <% end %>

      <!-- VIDEO SECTION
      ================================= -->
      <%= render :partial => 'cras/video' %>

      <!-- TEAM
      ================================= -->
      <%= render :partial => 'cras/team' %>


      <!-- PRICING SECTION
      ================================= -->
      
       <%= render :partial => 'cras/aboutsumasika' %>

      <!-- TESTIMONIALS
      ================================= -->
      <% unless @trusted.empty? %>
        <%= render :partial => 'cras/testmonial' %>
      <% end %>

      <!-- CLOSING
      ================================= -->
      <%= render :partial => 'cras/closing' %>